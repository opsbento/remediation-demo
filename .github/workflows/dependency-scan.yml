name: Dependency Scan

on:
  workflow_dispatch:
  pull_request:
    paths:
      - package.json
      - package-lock.json

permissions:
  contents: read

jobs:
  scan:
    uses: opsbento/security-workflows/.github/workflows/dependency-scan.yml@main
    with:
      working-directory: "."
      minimum-severity: high
